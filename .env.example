DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=snapp
DB_USERNAME=root
DB_PASSWORD=
DB_CHARSET=utf8mb4
DB_TIMEOUT=5

SECRET_KEY=kvpFWQDecn

# JWT / Auth configuration
JWT_SECRET=kvpFWQDecn
JWT_ALGO=HS256
JWT_TTL=60
JWT_REFRESH_TTL=43200
JWT_BLACKLIST_ENABLED=false
EITAA_BOT_TOKEN=

UPLOAD_PATH=http://localhost/taxi/snapp_api/uploads/

# Storage configuration
STORAGE_DRIVER=local  # local or s3
LOCAL_STORAGE_PATH=uploads
LOCAL_STORAGE_URL=http://localhost:9501/uploads

# AWS S3 configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_CDN_URL=  # Optional: CloudFlare CDN URL
AWS_ENDPOINT=  # Optional: for MinIO or custom S3
AWS_USE_PATH_STYLE_ENDPOINT=false

# Cache configuration
CACHE_DRIVER=redis
CACHE_TTL=300
CACHE_PREFIX=tigerapi:

# Redis configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Payment gateway (Saman / SEP)
APP_URL=http://localhost:9501
PAYMENT_ONLINE=0
# شماره پذیرنده از بانک سامان (اجباری برای پرداخت واقعی)
SAMAN_ID=
# یا با نام دیگر:
# SAMAN_TERMINAL=
# برای وریفای و برگشت تراکنش (در صورت نیاز)
# SAMAN_USERNAME=
# SAMAN_PASSWORD=